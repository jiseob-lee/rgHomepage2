<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rg.index.IndexMapper">

	<insert id="insertAccessLog" parameterType="com.rg.index.dto.IndexDTO">
		INSERT INTO access_log
		(
			user_agent, 
			x_forwarded_for,
			proxy_client_ip,
			remote_addr,
			hash,
			referer,
			language,
			country,
			city,
			subdivision
		)
		VALUES
		(
			#{userAgent},
			#{xForwardedFor},
			#{proxyClientIp},
			#{remoteAddr},
			#{hash},
			#{referer},
			#{language},
			#{country},
			#{city},
			#{subdivision}
		)
	</insert>

</mapper>